
.answers_new
  %h2 返答フォーム
  .answers_new-content
    - if @answer&.errors&.any?
      - @answer.errors.full_messages.each do |error|
        %p= error
    - else
      .answers_new-content-contact
        %p= "#{@comment.created_at.strftime('%Y/%m/%d %H:%M') }  着信"
        %p= "#{@comment.name}様 > #{@comment.text}" 
      = form_with(url: comment_answers_path(@comment.id), method: :post, local: true) do |form|
        = form.text_area :answer, placeholder: "返答内容" ,rows: 5
        = form.submit "送信"